{
  "name": "@ragenai/rag-core",
  "version": "1.0.0",
  "private": true,
  "description": "Vector-store contract shared by apps/web, apps/api and apps/worker (ADR-26)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "lint": "eslint",
    "typecheck": "tsc --noEmit --incremental --tsBuildInfoFile ./typecheck.tsbuildinfo"
  },
  "devDependencies": {
    "@ragenai/eslint-config": "*",
    "eslint": "^9.39.5",
    "typescript": "~5.7.0"
  },
  "engines": {
    "node": ">=24"
  }
}
