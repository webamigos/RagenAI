{
  "name": "@ragenai/rag-core",
  "version": "1.0.0",
  "private": true,
  "description": "Vector-store contract shared by ragen-app, apps/api and apps/worker (ADR-26)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "~5.7.0"
  },
  "engines": {
    "node": ">=24"
  }
}
