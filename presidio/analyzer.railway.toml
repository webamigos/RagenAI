[build]
builder = "DOCKERFILE"
dockerfilePath = "presidio/Dockerfile.analyzer"
watchPatterns = ["presidio/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
