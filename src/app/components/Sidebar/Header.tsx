import { SidebarHeader } from '@salesyy/common-ui';

import { Logo } from '../Logo';

export const Header = () => {
  return (
    <SidebarHeader className="mx-4">
      <Logo />
    </SidebarHeader>
  );
};
