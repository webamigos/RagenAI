/**
 * The height of the chat's top bar, shared by every bar that sits on its line:
 * the conversation header (breadcrumb, assistant selector, actions) and the
 * sources rail's header beside it. Each used to size itself from its own
 * padding, so "Sources" sat a few pixels off the breadcrumb and the two
 * borders did not meet.
 */
export const CHAT_BAR_HEIGHT = 'h-14';
