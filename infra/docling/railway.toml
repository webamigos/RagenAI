[build]
builder = "DOCKERFILE"
dockerfilePath = "docling/Dockerfile"
watchPatterns = ["docling/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
