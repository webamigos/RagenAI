[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docling/Dockerfile"
watchPatterns = ["infra/docling/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
